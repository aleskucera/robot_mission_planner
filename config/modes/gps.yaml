# mode: gps -- follow route waypoints only, ignore road
/**:
  ros__parameters:
    mode: "gps"
    # Disable road-loss fallback (no road observations subscribed)
    road_path_timeout: 0.0
    # Whole remaining route in one sequence
    gps_sequence_window: 0
