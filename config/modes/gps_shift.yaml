# mode: gps_shift -- the route's waypoints one at a time, each moved onto the road centre seen
# in road_map_2 (translation only); pure GPS around intersections.
/**:
  ros__parameters:
    mode: "gps_shift"
    # No ROAD state to fall back from
    road_path_timeout: 0.0
